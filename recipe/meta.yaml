{% set name = "opentelemetry-resource-detector-azure" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/opentelemetry_resource_detector_azure-{{ version }}.tar.gz
  sha256: 7a82d1c1794a3b252de9b0cb393116ae260f8414a085010e1c334cea34c4677e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - opentelemetry-sdk >=1.21,<2.dev0

test:
  imports:
    - opentelemetry.resource.detector.azure
    - opentelemetry.resource.detector.azure.app_service
    - opentelemetry.resource.detector.azure.functions
    - opentelemetry.resource.detector.azure.vm
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/open-telemetry/opentelemetry-python-contrib/tree/main/resource/opentelemetry-resource-detector-azure
  summary: Azure Resource Detector for OpenTelemetry
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
    - conda-forge/opentelemetry-api
